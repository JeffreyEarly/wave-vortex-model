# RK78 ordered combination screen (#493)

User authorized the bounded RK78 experiment. Base is merged PR #492 (`5b5ee65e`), with exact accepted runtime executable at `0a819a49`. Its frozen binary SHA256 is `c9da6aff33d46560a5bb545d1add21a0d37aa0382c93f67dc7bc53c5e5d092ee`. Worktree `wvm-v4-rk78-combinations`; v5 and experiments remain untouched. No new goal object was requested.

Shared profiling/caching/documentation guides, repo AGENTS and optimization workflow were read. The spectral benchmark history contains no RK combination experiment; WVM #185's rejected fixed-RK4 materialization change is a distinct design and remains rejected. One provider writer and one independent test/review worker assist; root schedules compute.

The first candidate fuses only RK78 stage 3–13 and accepted-candidate weighted combinations. It retains the materialized stage, term order, final h multiplication, all constraints and RHS calls, controller/error norms, stage reuse, and dense extension. Compile-time span helpers support coefficient families and additional real/complex blocks without allocation or threads. Aim for a few-percent model gain; stop if the complete model does not improve credibly.

## Verification ledger

- Fresh 20-second sample of the exact #492 binary on a disposable extended EddyTide continuation passes source/binary/input hashes and duplicate-work checks. RK78 exclusive main samples: 1,286 / 13,873 = 9.27%; this includes more than combination loops and is not an elapsed-time gain prediction. The coefficient state has three families of 18*11439=205,902 complex values each.
- Independent review checked every replaced stage term and reused-buffer lifetime. Focused tests compare real/complex helper results against separately materialized noinline legacy passes, N=2..9, signed zeros/cancellation, odd tails, and permitted exact helper aliases. Existing unified integration tests cover layouts, MATLAB fixtures, rejection, restart, dense output and storage.
- Fresh Clang unified integration, output orchestration and QG integration pass 3/3. The standalone GCC command initially selected an obsolete command-line-tools SDK and failed inside Apple headers; selecting the same Xcode SDK as the qualified GCC CMake build resolves compilation. No source workaround was added. GCC runtime checks and bounded timing screens follow.

Artifacts: `OceanKitRepositories/wave-vortex-model-benchmark-artifacts/rk78-combinations-20260912`. Check disk capacity and host idleness before timing. Preserve failures and do not repeat successful broad checks for evidence-only changes.

- GCC portable runtime/core and unified-integration test pass. The normal test target hit an unrelated existing Apple Mach-header failure while building its unused CLI dependency; `TestWVUnifiedIntegration/fast` linked the freshly built GCC libraries and passed when executed directly. The full CLI remains for Linux CI. Logs retain both results.
- First isolated screen: Clang passes exact bits at 617,706 and 2,000,003 values. Complex fused/legacy median ratios are 0.47–0.58; real ratios 0.38–0.57. GCC aborts on a bitwise mismatch before timing. Investigating contraction and adding non-unit-first-weight coverage before adoption; no tolerance relaxation. The Clang-only whole-model exploratory screen can proceed independently because it also checks exact outputs and step controls.

- One-pass Clang EddyTide pilot failed exact normalized-error equality on its first pair (all 96 steps); accepted times, step sizes and RHS counts matched. The 16.959/15.906-second timing is rejected evidence, not a qualified speedup. GCC disassembly isolated reversed first-product contraction. Revised helper restores a stored first product, then fuses the remaining updates and final affine into a second traversal. The private destination is disjoint from every source; all runtime call sites satisfy this. Independent tests now retain non-unit first weights and use separate noinline oracle passes, removing speculative alias support.
- Revised two-pass standalone Clang/GCC exact checks pass at 617,706 elements, all real/complex N=2,4,8,9. Fresh Clang unified test passes after rebuilding the actual runner. The old output/QG binaries were rerun but are not claimed as verification of the revised runtime; the final combined build will relink them if the model screen passes.

- The two-pass full-model gate also rejected exact normalized errors. Baseline self-comparison then proved this gate was stricter than the existing numerical contract: two runs of the identical frozen binary differ by up to 1.02635e-17 in coefficients; two-pass candidate versus baseline differs by up to 1.00331e-17. The inherited manifest already documents native-backend repeat variation and uses relative 1e-10 plus absolute 1e-12. Reinstated that existing whole-model gate and exact actual step decisions (excluding the error estimate, as in prior qualification). This is distinct from the real GCC helper contraction defect, which was fixed and remains checked bitwise. Failed gate records and outputs are retained; no scientific tolerance was relaxed from the accepted baseline contract.
- Revised GCC runtime library and unified-integration executable were freshly rebuilt and pass, including the stronger non-unit first-product regression. Revised standalone checks pass under both compilers; full timing follows after compiler work completes.

- Three-pair revised exploratory screen passes prior scientific tolerances, exact state/step/producer controls and unchanged memory. EddyTide ratios: 0.94870, 1.00408, 0.97340; geometric mean 0.97513 (2.49% improvement). Continue to one final combined correctness cycle and frozen campaign; this pilot alone is not a performance claim.
- Final scope: eight alternating measured pairs after two warmup pairs on EddyTide and the existing larger 256x256x129 Boussinesq fixture switched to RK78 for four capped 0.5-second steps. This tests the actual changed integrator in both MM models; unchanged RK4 transform campaigns are not repeated. Composite layouts and other solvers remain covered by unified tests and six-family forward/restart qualification. No tracer-specific optimization or low-memory policy work.

- Final combined release build and CTest pass 58/58. Fresh ASan/UBSan build and affected unified integration/output/QG checks pass 3/3 with macOS-supported `detect_leaks=0`. Both compiler arithmetic gates pass. Runtime source is ready to freeze; next relink runner/probe with exact frozen revision, collect affected MATLAB and six-family forward evidence once, and run the final performance campaign.
