# Issue #477 adoption ledger

The owner authorized completing qualification and merging both optimization directions. Previous experiments, native 58/58, seven focused sanitizer cases, GCC kernel/operator checks and seven MATLAB scientific comparisons remain in the linked exploration report. Prior benchmark-history review and independent lifecycle/numerical review are reused.

- Preflight: branch clean at 6eff86c2; main 5a41977b contains merged PR #476. No production experiment activity; unrelated processes below 7% CPU. Frozen compiled inputs matched e8806710 before the boundary correction below.
- A final factory audit found that SIZE_MAX vertical workers let std::length_error escape. The regression test reproduced the error before the fix. A narrow catch now returns sizeOverflow and preserves existing/null output ownership. Release, ASan/UBSan and GCC operator tests pass. Successful factory execution and numerical producer code are unchanged, so prior family/scientific coverage is reused; required CI will check the complete candidate.
- Stopped only this task's initial MATLAB receipt run before editing source. Its partial four-family results are retained at /private/tmp/wvm477-forward-6eff86c2 and the log /private/tmp/wvm477-forward-adoption.log. They are not claimed as final source-linked qualification. The final run uses a new evidence directory.
- Corrected the current runner CMake source hash; preserved unrelated historical baseline hashes.
- The final source-linked run covers six families and both providers, collects both inventories, then runs the single strict committed-receipts catalog check. Compatibility generation and documentation regeneration are unnecessary because catalog/scientific MATLAB/docs sources are unchanged.

Pending: final source-linked receipts, frozen idle-host campaign (two warmups/eight pairs, reuse), required hosted CI, merge and post-adoption profile. The coordinator exclusively schedules local compute; the Sol reviewer performs bounded read-only audits.

- Final source-linked run at 5947ac0d passed all six methods (both providers per method) and the strict committed-receipts check. Both inventories and 12 new content-addressed fragments are committed; old fragments are preserved. The final campaign will run a reconfigured runner at the receipt commit, with unchanged compiled numerical inputs.
