# Issues 396 and 315: forcing-tendency diagnostic increment

Branch: `issue-315-forcing-tendency-diagnostics`, based on v4 main `866f66ed419829c751a020646a6f3799bbe7e531`.

## Verification ledger

- #396: normalized RK78 dense-output coefficient alias checks through the integration layout's existing coefficient-family views. Regression coverage exercises legacy/explicit accepted states against legacy/explicit output states, each independently aliased coefficient family, unchanged endpoints, and rejection before evaluation or output mutation.
- #396: Apple Clang ASan/UBSan `output-orchestration` passed, including the pre-existing lazy extension, transactional retry, and accepted-trajectory comparison. Build: `/private/tmp/wvm395-standalone-sanitized`, rebuilt from this branch. Leak detection is disabled locally because this macOS sanitizer does not support it; address and undefined-behavior checks remain enabled.
- #396: Release `output-orchestration` passed using the combined CI build in `/private/tmp/wvm395-release`.
- No MATLAB source or scientific formulas changed in the RK78 fix.
- #396 merged through PR #403 on 2026-09-08 after all selected required checks passed (C++ release/sanitizer and MATLAB R2025b/R2026a). Main commit: `26dcfb5afea596b54c3e71500546cd610ee96f4d`; issue #396 is closed. No optional Full campaign was required.
- #315 kernel foundation: constant-stratification, Hydrostatic and Boussinesq nonlinear kernels can copy their raw spatial tendencies into caller-owned output before projection. Hydrostatic and Boussinesq can also consume an event's prepared physical fields; constant stratification uses its existing prepared-advection-field path. Default calls preserve their existing arithmetic and storage.
- Release `WVKernelContract`, `hydrostatic-kernel`, and `boussinesq-kernel` passed. Matching Apple ASan/UBSan tests passed (3/3). Tests compare raw products to independently requested derivatives, require unchanged projected flux and input state, reject invalid output shapes/aliases, and verify field reuse and unchanged retained kernel storage. Hydrostatic/Boussinesq allocation probes also observe zero allocations during prepared capture.
- Wave forcing engines now evaluate ordered per-instance contributions through the existing resolved instances. Spatial accumulation is projected once at the stage boundary; spectral/amplitude outputs reconstruct after-minus-before. Caller outputs commit together after successful evaluation. Unknown extensions reject before allocation unless their implementation explicitly supports diagnostics.
- Release `forcing-tendencies` passed for constant hydrostatic/nonhydrostatic, Hydrostatic and Boussinesq engines. Matching Apple ASan/UBSan `forcing-tendencies` and `forcing-and-rk4` passed (2/2). Coverage includes actual names and execution order, selected-only prefix evaluation, physical-field reuse, untouched model coefficients/RHS, unchanged persistent bytes, zero live event storage, invalid bindings/shapes/aliases, unsupported extensions, and late FFT failure with unchanged outputs and successful retry.
- A dedicated regression holds only the inertial Ap coefficient, without its Am partner. MATLAB's Fourier inverse discards self-conjugate imaginary values. Diagnostic reconstruction now averages the temporary inertial pair to preserve that real contribution; ordinary state constraints and validation are unchanged. Both Hydrostatic and Boussinesq cases pass.
- MATLAB R2025b `TestPortableStableForcing/fullGridWaveTendenciesMatchMatlab` passed with the reference provider: four wave configurations, odd/even rectangular grids, antialiasing on/off, and 10/11-force compositions covering every stable wave forcing. All 588 field comparisons satisfy 1e-12; maximum measured normalized error is `5.15e-13`. Fixed amplitudes include the unpaired inertial Ap case.
- The tidal Fw channel cancels analytically. Its measured discrepancies were approximately 1e-26, so relative error against the residual alone was undefined or misleading. This channel uses the separately reconstructed Ap/Am physical contribution magnitude as its conditioning scale. The test independently verifies analytic cancellation and bounds both MATLAB and C++ residuals, and their difference, at 1e-12 of that scale. Nonzero channels retain their ordinary per-channel relative bound.
- The native FFTW/Accelerate build also passed the same MATLAB method for reference and native providers (1,176 comparisons). Native maximum normalized error was `4.01e-13`; reference maximum remained `5.15e-13`. MATLAB Code Analyzer reported no issues in the changed test.
- No MATLAB runtime source, scientific formulas, public APIs, or saved-file behavior changed; only the parity test was extended.
- Actual-name field-plan and observer/NetCDF integration, QG support, individual and additional ordered compositions, sampling/output, and performance qualification remain outstanding. The catalog remains pending #315; these tests do not establish end-to-end completion.

### Barotropic QG checkpoint

- The same diagnostic sequence now handles one-family QG state and the existing resolved barotropic forcing instances. The four spatial kernel operations can capture their raw output instead of projecting each contribution; the diagnostic sequence projects their cumulative stage once. The ordinary RHS follows its existing path.
- Raw diagnostic Fourier inversion preserves horizontal means and takes the real part at self-conjugate modes. Ordinary QGPV reconstruction still masks the mean. A dedicated nonzero-mean test distinguishes these behaviors.
- Release and Apple ASan/UBSan `forcing-tendencies` and `barotropic-qg-forcing` passed. Tests cover independent nonlinear products, stage accumulation, one shared physical reconstruction, exact unchanged state/later RHS, unchanged retained storage, selected-only evaluation, late FFT failure and retry, raw/state alias rejection, and nonfinite-state rejection. The existing wave cases run alongside QG to cover the shared sequence change.
- MATLAB R2025b `fullGridBarotropicTendenciesMatchMatlab` passed for barotropic and equivalent-barotropic (`j=0/1`) configurations, odd/even rectangular grids, antialiasing on/off, and six/seven-force compositions. Both reference and native FFTW passed 104 field comparisons; maximum relative error was `2.342e-14`. No MATLAB runtime code changed.
- Discovered and reproduced MATLAB stratified-QG `SpatialForcingOperation` using `[Nz,Nkl]` Fourier rows at a boundary that requires `[Nj,Nkl]` modal rows. With `[8,6,9]`, `Nj=4`, and nonlinear plus adaptive forcing, MATLAB R2025b raises `MATLAB:sizeDimensionsMustMatch` at `WVAdaptiveDamping.m:334`, called from `SpatialForcingOperation.m:63`. The inverse side also omits vertical reconstruction. Reported as [#404](https://github.com/JeffreyEarly/wave-vortex-model/issues/404), with reproduction and compatibility requirements. Ordinary stratified-QG `nonlinearFlux` uses the correct vertical projection. This existing MATLAB defect must be explicitly represented in the eventual #315 qualification; it does not justify silently weakening comparisons or changing MATLAB behavior.
- MATLAB Code Analyzer passed for the updated parity test, and whitespace checks passed. Source changes after the numerical comparisons only add invalid-state/storage rejection; the positive numerical paths are unchanged.
- Stratified-QG numerical diagnostics and all end-to-end field/observer/output work remain incomplete. The full #315 goal remains active.

### Stratified QG checkpoint

- Stratified QG now uses the shared ordered diagnostic sequence and existing resolved forcing instances. Nonlinear advection, linear/quadratic bottom drag and vertical diffusivity expose raw spatial contributions through the existing kernel operations. Nonlinear advection and drag borrow one invocation's u/v fields, which beta advection and adaptive damping also reuse. The ordinary RHS retains its previous numerical path.
- The diagnostic stage boundary projects through the vertical F basis. Spectral contributions reconstruct through that basis and the real Fourier inverse, including horizontal means. Bottom drag diagnostics retain their raw bottom-cell profile instead of spreading it through the truncated basis.
- Release `stratified-qg-kernel` and `forcing-tendencies` passed; matching Apple ASan/UBSan tests passed (2/2). Coverage includes unchanged state/RHS/retained storage, shared velocities, exact instance order, raw bottom profiles, mean-preserving diagnostic inversion versus the unchanged ordinary mean mask, selected-only evaluation, late FFT failure and transactional retry, and raw/state alias rejection. The test's deliberately non-inverse modal matrices require building its independent expected accumulator in coefficient space; reprojecting reconstructed spectral deltas would be an invalid oracle.
- MATLAB R2025b `fullGridStratifiedTendenciesMatchMatlabPrimitives` passed for antialiasing on/off and odd/even rectangular grids with Nz greater than Nj. The reference/native matrix has 60 comparisons: maximum relative error `4.587e-14` reference and `2.656e-14` native. Code Analyzer passed.
- This method explicitly verifies the current #404 wrapper error, then uses MATLAB's individual forcing methods, `transformQGPVToWaveVortex`, and `transformToSpatialDomainWithF` as the numerical reference. It does not claim agreement with the broken wrapper and does not change MATLAB runtime behavior. #404 remains open and must remain explicit in the final per-row qualification.
- Raw prepared-velocity capture also leaves both the prepared fields and the unused spectral output unchanged; invalid prepared channel shapes reject before FFT execution.
- All five C++ forcing engines now have numerical diagnostic entry points. The goal is not complete: field/observer/NetCDF binding, shared work with other event fields, isolation and sampling/output/restart coverage, catalog qualification, and the representative performance budget remain outstanding.

### Field-service binding checkpoint

- `WVFieldEvaluationService` can borrow each of the five existing forcing engines. The binding retains immutable instance views, actual graph ordinals, and source-linked capability/dependency declarations. It resolves sanitized names before evaluation and rejects ambiguous names, unavailable channels, unsupported sampling, and unqualified forcing prefixes. Numerical execution still calls the same resolved instances.
- `WVDiagnosticFieldPlan` evaluates each selected contribution once and shares its channels and repeated outputs. Graph ordinals deliberately differ from execution indices in the regression fixture. Forcing fields retain their cataloged full-natural-grid restriction: profile and position requests reject. Ordinary grid, position, supported profile, and scalar outputs can coexist with forcing diagnostics.
- Physical dependencies are unioned with ordinary event fields. Wave engines borrow prepared u/v/w (constant stratification) or u/v/w/eta (modal transforms); QG engines borrow prepared u/v. Tests assert that the forcing engine does not reconstruct those fields again. Stratified QG still reconstructs its necessary QGPV derivatives and linear-drag vorticity. Spectral-only filtering does not request unnecessary physical state fields.
- Prepared fields are invocation-scoped and validated for shape, finite values, and state/output overlap before numerical execution. The ordinary RHS path is unchanged. Tests verify identical coefficients and subsequent RHS values after mixed field-service evaluation and retry.
- All requested outputs remain staged until the full evaluation succeeds, including ordinary fields and scalar reductions. Late FFT failures leave every caller buffer unchanged, free diagnostic workspace, and reproduce the successful result on retry.
- Retained-byte accounting now includes the small borrowed binding and immutable index/dependency plans. Tests check unchanged service/plan retained bytes across success, failure and retry, with zero live diagnostic bytes afterward. Per-invocation peak accounting avoids adding a historical, larger engine peak to a smaller event.
- Release `forcing-tendencies`, `field-evaluation`, `barotropic-qg-forcing`, `WVBarotropicQGKernel`, `observer-output-evaluation`, `hydrostatic-runtime`, and `boussinesq-runtime` passed (7 focused tests). The forcing/runtime/barotropic source-policy checks passed (3). Apple ASan/UBSan passed the forcing-tendency, field-evaluation, barotropic forcing, and barotropic kernel tests (4); local leak detection remains disabled for the previously documented macOS limitation.
- No MATLAB files changed in this checkpoint. The earlier MATLAB numerical comparisons remain recorded above; these C++ tests establish field-service equivalence to those numerical entry points, not final observer/NetCDF MATLAB qualification.
- Still required: actual-instance observer schema and NetCDF metadata, configuration preflight, output/restart/rebind and trajectory coverage, per-force isolation and per-catalog-row qualification (including #404), native-provider end-to-end qualification, and the representative runtime/retained-memory budget. #315 and the combined goal remain open. No optional Full CI was run.

### Observer and NetCDF checkpoint

- The existing forcing catalog now declares a data-only diagnostic metadata capability. Its binding resolver uses the same registered transform factories and ordered schedule identities without constructing forcings or FFT plans. Restored output configuration passes the inspected forcing schedule and transform configuration into observer schema preflight.
- Eulerian observer schemas use the configuration-specific forcing contract and substitute the actual, unsanitized instance name into its MATLAB description. Variable names retain MATLAB's space/hyphen replacement. Runtime and preflight schema manifests agree byte for byte across all five engines; preflight executes no FFTs. Template names remain subject to instance binding even after catalog delivery status changes.
- Two observers requesting the same contribution share one numerical evaluation. All five engine tests exercise observer batches, a late FFT failure with no available stale occurrence, and successful retry after rebinding an equivalent field service. Rebinding and initial preparation reject while an event awaits completion. Rebinding also rejects changed instance names or graph ordinals that would invalidate persisted metadata.
- The observer's forcing-containing output plan allocates its real/complex result arrays only for the current occurrence. Completion and failed preparation free those arrays and reset borrowed views. Their active capacity is included in occurrence workspace metrics; tests assert zero forcing result-array capacity before events and after completion/failure/retry. Immutable metadata remains retained.
- Barotropic QG NetCDF tests now request `Fqgpv_nonlinear_advection`. The grid was expanded from 5-by-4 to 7-by-6 because the smaller antialiased fixture retained no interacting modes and produced identically zero nonlinear forcing. The new test requires a nonzero reference and compares the last saved forcing field, before and after append, with an independent `-(u*q_x+v*q_y)` calculation at 1e-12. It verifies `[t,y,x]`, units `s-2`, and the exact MATLAB description.
- All four integrators (fixed RK4 and adaptive RK23/RK45/RK78) pass with dense retention enabled and interior output every 0.003 seconds versus base steps up to 0.005 seconds. Accepted states, accepted/rejected step counts and base RHS evaluation counts agree exactly with a run that has no output. Tests also cover restored forcing/schema identity, append, segmented continuation, and coincident sibling routes. This is C++ barotropic qualification; it does not establish the remaining wave/SQG or MATLAB/native end-to-end matrix.
- Release passed seven focused tests: forcing tendencies, observer output evaluation, barotropic model output, extension catalog, model output configuration, model output NetCDF, and model facade. Four relevant architecture/dispatch source-policy checks passed. Apple ASan/UBSan passed forcing tendencies, observer output evaluation, and barotropic model output (3); the previously documented local leak-detector restriction is unchanged. No MATLAB files changed and no optional Full CI ran.
- Remaining implementation gaps found by inspection: linear-mode model restoration currently clears the forcing schedule, so diagnostic instances must be preserved while keeping linear RHS behavior; the observer's common time-series plan still evaluates requests for inactive routes and separately evaluates moving/event plans, so active-route selection and cross-plan physical sharing need qualification and, where needed, correction.
- Remaining completion evidence includes all-engine MATLAB/native saved-output and dense-value comparisons, force-specific sink failure/retry, per-force isolation and per-row qualification (including #404), the representative 3% runtime/retained-memory budget, final required CI, and integration. The catalog, #315, and combined goal remain pending.

### Linear restoration checkpoint

- Restored linear models now retain their resolved forcing instances and identities. A construction-resolved linear mode skips only ordinary coefficient RHS contributions; diagnostic evaluation and fixed-amplitude restoration keep using the same instances. MATLAB's constructor omits coefficient flux integration in linear mode, while `integrateToTime` still calls `restoreForcingAmplitudes`. No MATLAB runtime or test source changed in this checkpoint.
- All five engine regressions require zero linear coefficient RHS, unchanged input coefficients, preserved fixed-amplitude constraints, and unchanged forcing diagnostics through field/observer evaluation, including transactional failure/retry. QG tests assert that linear RHS does not call any forcing. Constant stratification also verifies its shared physical advection context remains available. Returning to nonlinear mode reproduces the earlier RHS exactly.
- The new barotropic linear NetCDF regression exposed a C++ reader inconsistency: initial-only A0 without a WVCoefficients observer was recognized for stratified QG but rejected for barotropic QG. Both QG transforms now recognize that layout when the validated linear-dynamics attribute is true. Nonlinear QG restart ownership remains tied to the coefficient observer.
- A barotropic linear initial file now restores and appends twice, to 0.02 and 0.04. Every coefficient remains exactly equal to its initial value, each particle/tracer block evolves, nonzero saved forcing agrees with the independent raw nonlinear product, and reinspection preserves the linear flag and actual forcing identity. This is a C++ output/restart regression, not a new MATLAB/native saved-output qualification.
- Release passed six focused tests (forcing tendencies, barotropic model output, model facade, checkpoint reader, model output NetCDF, and model output configuration) plus three forcing/runtime/barotropic source-policy checks. Apple ASan/UBSan passed forcing tendencies, barotropic model output, and checkpoint reader (3). Local leak detection remains disabled for the already documented macOS restriction. Whitespace checks passed; no optional Full CI ran.
- The previously recorded linear-restoration implementation gap is resolved. Active-route selection and cross-plan physical sharing, the remaining MATLAB/native output and dense-value matrix, force-specific sink retry, per-row qualification and isolation, the representative performance budget, and final required CI/integration remain outstanding. #315 and the combined goal remain active.

## Diagnostic semantics established from MATLAB

`Operations/SpatialForcingOperation.m` reports the difference before and after each operation in stage/priority order. Spatial diagnostics expose the raw spatial contribution; spectral and amplitude diagnostics reconstruct the difference in the accumulated spectral tendency. Filters and fixed-amplitude operations therefore require the preceding accumulated tendency. Evaluating each forcing independently from a zero accumulator is incorrect.

The diagnostic execution must use the existing resolved forcing instances and coarse operation services. Instance identity and output metadata must be bound before evaluation. Scratch belongs to the observation occurrence, and diagnostic execution must not restore constrained amplitudes or change accepted integration state. Projection must preserve the distinction between raw spatial contributions and reconstructed spectral contributions.

#315 implementation and qualification remain in progress. This ledger is not completion evidence for #315.
