# MATLAB forcing diagnostics and portable request repairs

Scope: #404, #406, #408, and #410 on v4 main after #411 (`e799c6bc`). Preserve MATLAB APIs, saved-file contracts, and unaffected scientific results. #407 and #409 remain separate follow-ups. Work is isolated from the v5 checkout.

## Implementation and verification ledger

- Read the shared workspace policy, MATLAB style, annotated persistence, package design/release, and documentation guides. No repository-local AGENTS.md is present. No package snapshots, dependency metadata, or website content are being edited.
- #404: use the existing QGPV modal projection and F-basis reconstruction in `SpatialForcingOperation`; the barotropic implementations retain their Fourier-only behavior.
- #406: initialize spatial wave accumulators at the transform's grid shape and use the published variable name for a single-output QG operation.
- #408: restore missing standard forcing outputs before observer categorization, preserving registered operations, including partial user overrides.
- #410: pass dynamics mode into the existing compact barotropic restart validator; initial-only linear states are admitted while nonlinear states still require a declared coefficient observer.
- Replace primitive-reference operations, fresh-writer restart workarounds, and the QG-specific CLI path in `TestPortableStableForcing` with the repaired MATLAB operation, model restoration, and request helper.
- Initial focused tests passed all isolated forcing compatibility pairs and ordered SQG compositions on reference/native providers, the repaired isolated wrapper cases, and positive/negative barotropic request validation.
- All 12 direct output-continuation methods passed across six transform configurations, linear/nonlinear dynamics, both providers, and four fixed/adaptive/segmented scenarios: 96 scenario rows, 35,520 field-record comparisons, maximum normalized error 3.7952e-13. Actual MATLAB file restoration replaces the fresh-writer workaround; the common request helper replaces the QG CLI branch. Nonlinear runs retain the exact accepted-state/step/RHS comparison against their ordinary-output controls. A subsequent assertion-only edit requires warning-free MATLAB restoration; the final barotropic nonlinear matrix and single-output constructor case passed again after that edit and the scalar-check cleanup.
- Observer tests passed for fully registered operations, partial user overrides, and ordinary outputs that must not add forcing operations. Three-family MATLAB uninterrupted versus segmented restart/append passed with exact saved times, finite values within 1e-12, unchanged units/descriptions, warning-free restoration, and the actual SpatialForcingOperation instance restored.
- Linear barotropic requests passed real/split-complex initial states, explicit A0 output, and multiple output groups. Ambiguous, incomplete, unowned time-series and nonlinear-without-stream cases reject without changing the source or an existing request. Existing request transaction/failure-injection tests passed after removing a redundant post-close file-descriptor assignment flagged by Code Analyzer.
- The modal-basis regression passed odd/even horizontal grids with both a truncated basis and the maximum supported Nj=Nz-1, ordered forcing stages, unchanged A0, and a nonzero mean-mode diagnostic. The attempted Nz=Nj fixture exposed a separate explicit-operator constructor defect, reported as #412: the direct initialization guard always requires an absent positional Lz field. A reproduced equivalent preconditioning payload is ignored (maximum supplied/actual PF0inv difference 1). This batch does not repair that constructor or claim equal-dimension model construction.
- A temporary selection script initially concatenated two char patterns and selected zero observer tests; using a string array corrected the inventory before execution. The first modal fixture requested an unsupported count; only that failed method was rerun after correcting the supported scope. Passing matrix and observer tests were not repeated.
- All six explicitly selected v4 catalog tests passed; generated metadata and the 23 legacy records remain unchanged. Code Analyzer scanned all eight changed MATLAB files, then reran only the two files with findings after replacing a scalar-length check with isscalar and deleting the redundant assignment. No blocking findings remain. Existing suppressed test performance annotations are unchanged.
- One docs:check passed with 2,026 files, 4,145 routes, zero validation failures and zero generated changes. Repository boundary and whitespace checks passed. No C++ numerical source changed, so C++ performance qualification was not repeated; the direct reference/native interoperability matrix uses the previously qualified executable.
- Required CI and integration remain outstanding. No optional Full campaign is an ordinary merge gate. #407, #409 and #412 remain separate MATLAB follow-ups.
