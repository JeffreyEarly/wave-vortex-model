# Advisory API verification

- Base: merged PR #402, integration commit `6299ba351e65c8c104436cdc675a33b70eddb610`. Required hosted checks passed after preserving the scalar pilot MAT locally instead of tracking it. Issue #400 is closed; the integration checkout is synchronized and the merged branch is removed. All original study MAT data remain in the detached study worktree.
- Read applicable shared MATLAB, package-design/release, and documentation guides. No repository-local AGENTS.md adds requirements. This authoring increment uses existing dependencies and does not modify package exports, runtime defaults, website sources, persistence, or versioned snapshots.
- All twelve shared scientific tests and the initial six advisory tests passed after extraction. The reference-allowance validation and independent quadratic rejection test were then added; all seven final advisory tests passed. They cover frozen-study equivalence, explicit count rejection without replacement, fixed-family rejection, inconclusive references, budget rejection before projection, invalid requests, and independent Gram/quadratic decisions.
- A command typo in the first ancillary analyzer invocation occurred after the successful final tests. The corrected analyzer invocation completed: only two inherited `FNDSB` performance suggestions in the extracted helper, no other findings. Tests were not rerun for the command typo.
- The actual file-writing sparse survey client was replayed through the extracted engine. All 639,744 stored pair/output-prefix comparisons matched the preserved dense survey exactly; the maximum CSV prefix difference was 1.6653e-16. Product counts and reference/fixed-family statuses matched.
- One final documentation check passed with ClassDocumentation 1.3.2: 2,358 files, 4,819 routes, zero validation failures, and no generated differences. Later authoring Markdown/log additions do not affect generated website content.
- Compressed exact logs and a machine-readable verification summary are in `verification/advisory-api`. Temporary replay MAT files remain outside the repository. Final whitespace, package/snapshot/scope and clean-checkout checks passed before handoff.
- This delivers the authoring API increment. A direct model-instance adapter remains future integration because the constructor currently retains sampled mode arrays rather than this continuous/reference preparation. It must preserve the resolved modes and exact grid/count correspondence; no implicit re-solve is introduced here.
