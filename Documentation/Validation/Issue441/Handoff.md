# T10 decisions and bounded campaign follow-up

Use the [final T10 report](README.md) as the authority for the supported configuration, completed gates, missing references and resource measurements. The current conclusion is **NOT READY for a seasonal campaign**. This handoff proposes the next bounded scientific work; it does not authorize a seasonal production run. The existing [experiment E2 issue](https://github.com/JeffreyEarly/free-surface-qg-experiments/issues/2) requires a T10 READY configuration before its three-cycle qualification and longer extensions. A conditional result supports resolving named prerequisites, not treating that entry gate as satisfied.

## Configuration and decisions to retain

The candidate is **64×64×385, 257 complete thermal directions, four mean modes, 2,057 assembly points and 769 nonlinear product points**, retaining the fixed physical laws and both endpoints. Use the exact tested InternalModes beta.5 package graph recorded in [Reproduction.md](Reproduction.md). Revalidate later provider graphs before adopting them. Runner adoption should consume the existing transform, forcing, integration and annotated-persistence interfaces.

The fixed-space 769/1537/3074 comparison passes all 81 six-hour trajectory rows and all 36 initial-state checks on the named manufactured stress fixture. The scientific maps and physical laws are held fixed; only product sampling and its assessment metadata change. This supports the lower product rule on that representation and window, not arbitrary future high-mode states. The [closure/product audit](ClosureAudit.md) preserves the evidence and scope. The exact 769-point complete workloads are recorded in [Measurements.md](Measurements.md); dense-budget, provider-free restart and final acceptance outcomes remain owned by the main report. Completed 1537-point spatial comparisons retain their original identities and limitations.

**The main unresolved numerical entry gate is thermal-reference uncertainty.** The completed 257/385-direction comparison differs by at most **0.648431%** in QGPV, below its complete 1% spatial allowance, but the required 513-direction trajectory was stopped at the 32 GiB memory limit. Without that finer trajectory, all 81 lower-pair comparisons remain **INCONCLUSIVE REFERENCE**. This is neither a demonstrated failure of 257 directions nor a passing bandwidth qualification. See [Accuracy.md](Accuracy.md). Preserve the cutoff and resolve this gate through separately scoped work that fits the existing memory ceiling; do not retry with a larger cap or designate the 385-direction trajectory as an uncertainty-free reference.

The required 13-record diagnostic assessment is also **FAIL**. Both exact cold initial states exceed the locked 1% coefficient-relative criterion (53.45%/56.54% refinement changes in nominally zero APV coefficients), although all 36 physical observable comparisons for those two states and all 11 other record comparisons pass. Establish and test a justified near-zero coefficient criterion before claiming diagnostic acceptance; preserve the failed result. The separately accepted 769-point restored checkpoint has only conditional coverage. The cold M10 128-observation budget refinement stopped at its 16 GiB operational cutoff and contributes no completed refined budget. Cold M100 completed its refined assessment, but its potential-enstrophy budget remains **INCONCLUSIVE**. Both are unresolved entry gaps. See [Measurements.md](Measurements.md) for the exact rows, roundoff-scale source norm and missing diagnostic fallback basis. These are additional entry gaps, not grounds for increasing grid density or memory limits automatically.

| Decision | Reason and next condition |
| --- | --- |
| Prefer no optional damping | Use it while the named accuracy, budget and stability gates pass. Physical diffusion and bottom drag remain active. |
| Retain horizontal-only damping as a fallback | It passes the five-percent short-window bias screen, but barely acts on this fixture. A developed cascade requires its own bias/activity evidence before adoption. Preserve the same physical cutoff when transferring resolution. |
| Exclude the tested six-mode vertical APV taper from the pilot recommendation | It produces positive physical energy work and differences of 32.53% in QGPV and 11.68% in the upper-layer buoyancy gradient, exceeding the declared bias screen. Increasing coordinate damping is not a demonstrated remedy. A different energy-dissipating vertical closure would be a separate design task. |
| Keep offline diagnostics independent | Retain the candidate 64 APV modes on 129 stored diagnostic depths and 513 physical integration points. Its basis checks pass, but required cold-record coefficient acceptance still needs resolution. This does not change native evolution sampling or the six-mode historical damping definition. |
| Separate M10 and M100 decisions | The 0.1 m/s manufactured stress fixture reaches recorded displacement/depth 1.318. It tests numerical behavior outside a credible small-displacement seasonal interpretation and cannot stand in for mature M100 evidence. |

The matched cold control clarifies the speed tradeoff: 769 points reduce thermal median integration time by **35–37%** relative to 1537 points, but thermal integration still takes **1.48–1.50 times** the historical APV cold median wall time. Setup costs are separate, and these different discretizations have not established equal trajectory accuracy. The completed candidate thermal sample files remain about **696 MB**, including authoritative scientific arrays. Product sampling improves stepping cost without eliminating the cache/storage burden; accept that measured tradeoff separately from numerical readiness.

The separately retained 1,537-point baseline case construction, initialization and cache write took **33.653108875 seconds**; this is not pure eigensolver time or a measured new 769-point construction. Compatible cases use the stored arrays, and their restoration/integrator setup costs remain separate. The 63 saved-state applicability checks also support distinguishing initial conditions: cold M10/M100 displacement/depth stays below **6.70e-6** over their one-day controls, while all 27 saved manufactured M100 stress records have order-one displacement. These observations justify starting prerequisite work from the physical cold state; they do not establish long-term applicability.

The candidate's [conditional resource scenarios](Measurements.md#conditional-campaign-arithmetic) put combined serial M10+M100 integration at **6.400–6.768 / 31.999–33.841 / 127.994–135.364 wall hours** for 1/5/20 model years per case, using sample-output timings. Corresponding two-file initial-plus-logical-payload scenarios are approximately **1.663 / 2.794 / 7.034 GB**. Add measured startup once per case, not per output record. These control-derived ranges leave actual future allocated storage, checkpoint/analysis costs, mature M100 throughput and production resource limits unknown. They inform the next bounded measurements; they do not authorize those campaign durations.

For later user review, propose provisional ceilings of **seven serial wall days**, **20 GiB of allocated disk**, the existing **32 GiB memory reporting ceiling**, and **at most two rotating checkpoints per case** for a possible combined 20-year-per-case campaign. The measured-control extrapolation is 127.994–135.364 hours before unmeasured costs, and the two-file logical payload scenario is about 6.551 GiB. These proposed ceilings reserve room without demonstrating that mature throughput, production output cadence, checkpoint allocation or analysis overhead will fit. They are planning proposals, not accepted criteria, feasibility claims or permission to launch; the bounded prerequisite pilots retain their separately declared shorter wall and operational memory limits.

## Proposed bounded extension toward E2

Tracked prerequisites are [#530](https://github.com/JeffreyEarly/wave-vortex-model/issues/530) for diagnostic memory/reference completion and [#531](https://github.com/JeffreyEarly/wave-vortex-model/issues/531) for a justified near-zero diagnostic comparison. The separate shared restart failure-cleanup task is [#532](https://github.com/JeffreyEarly/wave-vortex-model/issues/532). These issues preserve the original T10 failed and inconclusive records.

1. **Close the named entry gaps.** Review the final report's exact 769-point no-optional-damping workload, budget and restart results, together with time, mean and spatial references. Preserve failed or memory-limited rows. Resolve each entry-blocking uncertainty through a separately bounded check: finer thermal-reference uncertainty, the cold near-zero diagnostic coefficient criterion, the incomplete cold M10 budget refinement and the inconclusive cold M100 potential-enstrophy budget. Do not promote passing physical diagnostic rows to overall acceptance under an unchanged failed rule. Freeze state/forcing identities, step policy and diagnostic basis before a new trajectory; reuse unaffected shared-interface verification.

2. **Start from the physical cold M10 case.** Use time zero, zero QGPV and mean, the deterministic 1 cm RMS surface seed and original annual source phase. Reuse matching completed cold-start checks. Proposed checkpoints are one day, seven days, then separately budgeted progress toward the first source maximum. Record each target and wall limit. Preserve the forcing year, diffusivity and seed; reaching one source maximum does not cover a full cycle.

3. **Qualify the states that emerge.** Save a small set around startup, nonlinear activity, source zero and source peak. Use the accepted advection-disabled companion trigger, occupied horizontal/thermal tails and triggered mean-mode control, retaining transfer/reference uncertainty. Diagnose actual records with the saved APV basis and refined quadrature, including both endpoints and physical cross terms. A capacity-only manufactured record does not fill a missing seasonal regime.

4. **Gate extension on budgets and applicability.** Retain denser budget observations where needed. Stop on nonfinite state/inventories, step underflow, ten consecutive rejections, or the configured memory/wall limit. More than 20% rejections flags throughput investigation. Hold scientific extension for an inconclusive required budget/reference or order-one displacement/depth, relative vorticity/f or surface slope. Record maxima as sampled diagnostics. Start a separate cold M100 trajectory only after reviewing M10 prerequisites and resources; the manufactured stress case supplies no mature-flow applicability evidence.

5. **Set production limits before expanding the horizon.** Campaign wall time, disk use and checkpoint/analysis overhead remain unaccepted. Propose limits from the exact candidate's startup and active-state measurements. Budget scalar `T/384`, coefficient `T/48`, denser budget observations and a small field-snapshot set. Separate logical payload from allocated growth, including checkpoint copies and analysis workspace. Three cycles and 1/5/20-year extensions remain separate E2 execution decisions.

## Memory and one narrow runtime follow-up

Run MATLAB serially in fresh guarded processes, release owned models/transforms before building another large cache, and retain accepted-state checkpoints persistently. Completed 64-grid measurements are roughly 15 GiB of sampled family footprint; the [physical metric cache](../../../@WVTransformFreeSurfaceThermalQG/physicalMetricOperators.m) is a major cost. The accepted 32 GiB reporting ceiling bounds declared probes; retain lower operational cutoffs where sufficient. A cutoff leaves a reference inconclusive. Cache compaction is separate from reporting this limitation.

One failure-only issue remains in [WVModel.modelFromFile](../../../@WVModel/modelFromFile.m): model construction at line 28 and the output-restoration catch at lines 35–39. If observer restoration throws, the factory closes its NetCDF handle but does not explicitly delete the partial model/transform; the caller never receives them. Add exception cleanup whose ownership is relinquished on successful return, with one forced-restoration-failure resource-release test. Preserve successful behavior, persistence format and provider-free restoration. This is a narrow fix, distinct from the passing continuation checks in [Audit.md](Audit.md).

Preserve historical APV pins and output directories throughout runner adoption. New product counts, closure choices, resolutions or physics require fresh case directories and provenance; never append a revised operator to an old trajectory. The finite-amplitude model investigation and any new vertical closure remain separate from this bounded E2 prerequisite work.
